name: Publish

on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: write
  pull-requests: write

env:
  AWS_REGION: eu-west-2
  AWS_ACCOUNT_ID: "094954420758"

jobs:
  build:
    if: github.run_number != 1
    name: CDP-build-workflow
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v5

      # Publishing an image whose tests never ran is worse than a slow publish.
      # verify, not test: Failsafe owns the integration tests.
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'corretto'
          java-version-file: '.java-version'
          cache: 'maven'
      - run: mvn clean verify

      - name: Build and Publish
        uses: DEFRA/cdp-build-action/build@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

  # Adds linux/arm64 to the Docker Hub tags the build job has just published.
  # Runs after it, never gates it, and never touches ECR.
  publish-arm64:
    needs: build
    uses: DEFRA/trade-imports-workspace/.github/workflows/publish-multiarch.yml@main
    with:
      image-name: trade-imports-plants-backend

  sonarcloud:
    name: SonarCloud Scan
    needs: build
    if: github.actor != 'dependabot[bot]'
    uses: ./.github/workflows/sonarcloud.yml
    secrets: inherit
